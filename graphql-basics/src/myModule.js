const message = `Hello from My module`;

const name = 'Andrew'

const location = 'Philadelphia'

const getGreeting = (name) => `Welcome to practice ${name}`;

export { message, name, getGreeting, location as default };